# 终端信道相关性交付报告

## 完成内容

- T-01：在 `tk8710_gw.c` 内实现 20-bit AH 符号扩展、复数共轭归一化相关性、高 3 byte 去重排序、上三角输出和追加文件日志。
- T-02：完成算法边界复核、差异检查及 RK3506 交叉编译链接。
- Git 集成状态：patch-ready；未 stage、commit、push 或创建 PR。

## 验证证据

| 检查 | 结果 |
|---|---|
| 20-bit 边界 | PASS：0、524287、-524288、-1 |
| 去重与组合数 | PASS：最后一条覆盖；16 终端为 120 对 |
| 相同、缩放、统一相移、正交向量公式复核 | PASS |
| `git diff --check` | PASS；仅输出工作树现有 LF/CRLF 提示 |
| RK3506 `./cmake/build_rk3506.sh tk8710_gw` | PASS；生成 `build_rk3506/tk8710_gw` |

构建仅保留原文件已有的 `SaveTxDcFromRfRam` 未使用警告，本次新增代码没有编译警告。测试夹具曾因错误地把单天线 `1` 与 `j` 当作正交、以及 PowerShell 有符号十六进制右移而失败；修正夹具后通过，未因此修改产品公式。

## 质量与风险

- 公共 API、协议、数据结构、安全和迁移质量门禁：N/A，本次为示例程序内部计算与本地日志。
- 文件创建/打开错误仅降级到控制台，不阻断 IPC 上报。
- 当前未连接板卡，尚未验证实际 AH 字节序、同批多终端运行结果以及 `/userdata/8710log/correlation.txt` 持久化；该项不影响源代码和交叉构建完成状态，但仍是硬件交付前必测项。
- 工作树在本任务前已有多处用户修改；本次未提交或清理任何既有变更。
