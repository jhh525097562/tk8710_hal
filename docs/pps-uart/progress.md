# RK3506 外部 PPS UART 管理进度

**更新时间：** 2026-09-02

| 阶段 | 状态 | 证据 |
| --- | --- | --- |
| 路由 | 完成 | 中量，governed planning path |
| 文档启动审批 | 完成 | 用户选择开始生成、集中审阅 |
| Phase 1 文档 | 完成 | 四份文档位于 `docs/pps-uart/` |
| Phase 1 Gate | 已批准 | 用户回复“同一”，按“同意”解释 |
| Phase 2 编排 | 完成 | `task-orchestration.md`，5 任务/5 批次/DAG 无环 |
| Git 安全 | 完成 | shared-working-tree serial + patch-ready，writer cap=1 |
| Phase 2 Gate | 已批准 | 用户回复“请继续” |
| Phase 3 执行 | 已结算 | B-01 至 B-04 完成；H-01 部分完成 |
| 最终验收 | ready-for-review | 软件和 `.62` 正常路径通过；异常板测及仪器波形未执行 |

# 当前工作树保护

- 保留 `test/example/tk8710_gw.c` 的既有修改。
- 保留整个未跟踪 `3506uart/` 目录。
- 本流程当前新增内容仅位于 `docs/pps-uart/`。
- 不允许 stage、commit、push、PR、merge 或 destructive Git 操作。

# Runtime Orchestration State

| Batch/任务 | 状态 | 证据 |
| --- | --- | --- |
| B-01 / T-01 | 完成 | 公共 API、默认配置及健康分类测试严格编译并通过 |
| B-02 / T-02 | 完成 | Linux 伪终端覆盖 EVENT/OK、状态、诊断、复位重连；测试通过 |
| B-03 / T-03 | 完成 | 监控程序严格编译；两个 RK3506 目标交叉构建成功 |
| B-04 / T-04 | 完成 | 严格测试、定向/全量交叉构建、符号、差异和质量审查完成 |
| B-05 / H-01 | 部分完成 | `.62` 正常路径通过；异常板测及示波器/计数器未执行 |

- 动态补充：新增 `TK8710PpsGetVersion()`，用于满足已批准的启动版本记录需求。
- TDD 修复：严格 C99 编译发现 Linux 特性原型不可见，已补充源文件特性宏并复测通过。
- Git 状态：patch-ready；未执行 stage/commit/push。
- 板端正常路径：`192.168.100.62` 运行 75 秒，命令顺序正确，进入并保持健康态，退出 0。
- 验收报告：`docs/pps-uart/delivery-report.md`。

# V1.1 网关 GPS/PPS 集成进度

| Batch/任务 | 状态 | 证据 |
| --- | --- | --- |
| B-06 / T-06 | 完成 | 策略测试和伪串口命令顺序测试通过 |
| B-07 / T-07 | 完成 | 普通/快速启动统一同步位；外部 ACM 立即触发；交叉编译通过 |
| B-08 / T-08 | 完成 | 启动探测、搜星、计算周期、对齐、运行监测和统一退出已接入 |
| B-09 / T-09 | 完成 | 严格回归、质量审查、双板正常路径和 V1.1 交付记录已完成 |

- 用户已批准 V1.1 实施方案，Phase 3 从 B-06 恢复执行。
- 固定策略：启动先探测 GPS；搜星 300 秒；10 秒轮询；连续 3 次判定；恢复退出码 2。
- 保留用户已有 `CONSOLE_POLL_INTERVAL_SEC=10` 修改和全部未跟踪资料。
- 动态补充：`.62` 无 NS 下发，新增仅测试宏 `TK8710_GPS_TEST_HOOKS` 注入固定 NS 配置；正式二进制已验证不含测试参数。
- 最终构建：7 个相关 RK3506 目标一次构建通过；仅保留网关既有 `SaveTxDcFromRfRam` 未使用告警。
- 板端证据：`.140` 无模块时进入本地同步并确认 `ls_en=0、ls_master=1`；`.62` 通过测试专用 NS 配置验证 7 秒计算周期、命令顺序、对齐、外部同步寄存器和周期监控。
- AG32 恢复状态：`.62` 已重新配置为 10 秒并连续 3 次读取到完整健康状态。
- 未执行：真实 UART/PPS 故障注入、保护进程退出码联动、示波器或计数器波形测量。

# V1.2 GPS API 测试场景注入进度

| Batch/任务 | 状态 | 证据 |
| --- | --- | --- |
| B-10 / T-10 | 完成 | 健康伪设备下三场景严格测试通过 |
| B-11 / T-11 | 完成 | 测试宏 API、恢复确认后一次性标记、网关 CLI 已实现 |
| B-12 / T-12 | 完成 | 严格回归、RK3506 交叉构建、正式目标隔离和 `.62` ARM 运行通过 |

- 需求基线沿用 `多网关GPS同步功能验证.md` 中已确认的 SIM-01、SIM-02A、SIM-02R；
  本次仅实现测试能力，不改变生产 GPS/PPS 行为。
- Runtime Orchestration State 已从实际工作树、任务编排、测试方案和本进度文件重建。
- Git 状态：shared-working-tree serial + patch-ready，writer cap=1；保留所有用户已有修改和未跟踪资料。
- 质量审查修正：恢复标记由“进入降级即写入”调整为“连续 3 次真实健康后、返回重启
  动作前写入”，避免进程提前退出时错误消费场景。
- 板端证据：`remote_logs/trm_run_20260907_145003_gps_fault_api_accept/`；自包含 ARM
  测试退出 0、未强制终止、无 ERROR/WARN。
- 尚未执行测试网关真实 300 秒等待和保护进程重拉；本批没有停止或替换板上正在运行的网关。
